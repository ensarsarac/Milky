@model List<ResultSocialMediaDto>
<div class="d-flex pt-3">
    @foreach (var item in Model)
    {
        <a class="btn btn-square btn-secondary rounded-circle me-2" href="@item.Url" target="_blank"><i class="@item.Icon"></i></a>
    }
</div>
